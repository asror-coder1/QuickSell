@FilterDef(name = "tenantFilter", parameters = @ParamDef(name = "shopId", type = java.util.UUID.class))
package com.quicksell.engine;

import org.hibernate.annotations.FilterDef;
import org.hibernate.annotations.ParamDef;
